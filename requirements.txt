# ─────────────────────────────────────────────────────────────────
#  Close-Scale — Python Dependencies
#  Python 3.13
# ─────────────────────────────────────────────────────────────────

# ── Django Core ───────────────────────────────────────────────────
Django==5.2.6
djangorestframework==3.16.1
django-cors-headers==4.9.0

# ── Database ──────────────────────────────────────────────────────
psycopg2-binary==2.9.10
dj-database-url==2.3.0

# ── ASGI Server (replaces gunicorn — required for WebSockets) ─────
daphne==4.1.2

# ── Django Channels (WebSockets / real-time) ──────────────────────
channels==4.2.2
channels-redis==4.2.1

# ── Celery (Background tasks) ─────────────────────────────────────
celery[redis]==5.5.3
django-celery-beat==2.8.1

# ── Authentication ────────────────────────────────────────────────
djangorestframework-simplejwt==5.5.0
django-allauth[socialaccount]==65.9.0

# ── File Storage (Cloudinary) ─────────────────────────────────────
cloudinary==1.42.1
django-cloudinary-storage==0.3.0

# ── Static Files (Render — WhiteNoise serves static files, no separate Nginx needed) ──
whitenoise==6.9.0

# ── Utilities ─────────────────────────────────────────────────────
Pillow==11.3.0
python-dotenv==1.1.1